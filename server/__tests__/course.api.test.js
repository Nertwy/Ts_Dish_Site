"use strict";
// import request from "supertest";
// import { app } from "../src";
// // describe("/checkAccessToken", () => {
// //   it("should return 200 and empty array", async () => {
// //     await request(app).get("/checkAccessToken").expect(200, []);
// //   });
// // });
// describe("/AddDish", () => {
//   it("Should return (ok true) if file send (ok falase) if file empty or wrong type", async () => {
//     await request(app).post("/AddDish").expect(200, { ok: true });
//   });
// });
